function set_tvs_volume() {
	/*var gain = global.channels * .0909;
	if (gain > .96) gain = 1;
	audio_sound_gain(tv_talking, gain, 0);
	audio_sound_gain(tv_talking, gain, 9999999);
	var pos =  audio_sound_get_track_position(global.tvSound);
	global.tvSound = audio_play_sound(tv_talking, false, true);
	audio_sound_set_track_position(global.tvSound, pos);*/
}